//Question 1 Create a loop that greets us 5 times!

//  for (var index = 0; index <5; index++){
//  console.log('Hi!');
// }




// Question 2 Create a loop which tells us what loop number it is on

// for (var i = 0; i < 5; i++) {
// console.log(`Loop Count ${i + 1}`);
// }


// var Array = ['Zeus', 'Hades', 'Hermies', 'Poseidon'];
// for(var index = 0; index < arr.length; index++){
// console.log(arr[index]);
// } var fullname = arr [0]


// Question 3 Loop over the following array and console.log the current index value
  //'Zeus', 'Hades', 'Hermies', 'Poseidon
 // var arr = ['Zeus', 'Hades', 'Hermies', 'Poseidon'];
  //for (var index = 0; index < arr.length; index++){
  //console.log(arr[index]);
  //}


 // var num = 1;
 // num = num + 1;
 // num = num + 1;
 // num = num + 1;
 // console.log(num);

 // for (let i = 0; i < 5; i+=1) {

//console.log(i);
 
//  }
//Intermediate

    // Iterate over a string & return all vowels

    // Iterate over a string & make every other letter capital

    // Iterate over an array & log any word that has the letter 'e' in it

//Advanced

    // Iterate over the sentence 'Peter piper picked a pickled pepper' & 
    // create a new string which has only the words with the letter 'i' in it.

    //Iterate over a string and log the string backwards
        //  Examples: 
        //          - 'this' => 'siht'
        //          - 'test' => 'tset'
        //          - 'Pizza' => 'azziP'

        var